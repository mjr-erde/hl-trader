# Agent wallet private key (64 hex chars). Used for signing trades only.
# Create at app.hyperliquid.xyz/API — trade-only, no withdrawals.
HYPERLIQUID_PRIVATE_KEY=

# Main wallet address (where USDC is held). REQUIRED when using an agent wallet.
# Also required for the web Live page. Balance and position queries use this address.
# If you use a single wallet (no agent), omit this — the signer address is used.
# HYPERLIQUID_ACCOUNT_ADDRESS=0x...

# ntfy.sh channel name for push notifications
# NTFY_CHANNEL=your-channel-name

# ntfy.sh access token for push notifications (optional but recommended)
# NTFY_TOKEN=tk_...

# LunarCrush API v4 key for social sentiment data (optional, advisory only)
# Get one at https://lunarcrush.com/developers
# LUNARCRUSH_API_KEY=...
